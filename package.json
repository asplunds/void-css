{
  "name": "void-css",
  "version": "1.0.31",
  "description": "A JIT & statically compiled CSS processor",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch ./**/*.ts --exec npx ts-node index.ts",
    "test": "jest --watchAll",
    "tsc": "tsc",
    "clean": "tsc --build --clean",
    "prepare": "npm run tsc",
    "postpublish": "npx tsc --build --clean"
  },
  "author": "Jonathan Asplund",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.16.0",
    "@types/jest": "^27.0.3",
    "@types/node": "^17.0.5",
    "babel-jest": "^27.3.1",
    "jest": "^27.3.1",
    "nodemon": "^2.0.15",
    "ts-jest": "^27.1.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}